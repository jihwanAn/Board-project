import React from "react";
import styled from "styled-components";

const Main = () => {
  return <Container>Main 페이지</Container>;
};

const Container = styled.main`
  display: flex;
  flex-direction: column;
  align-items: center;
`;

export default Main;
